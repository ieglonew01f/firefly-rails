<%= form_for(@online_user) do |f| %>
  <% if @online_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@online_user.errors.count, "error") %> prohibited this online_user from being saved:</h2>

      <ul>
      <% @online_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :is_online %><br>
    <%= f.number_field :is_online %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
